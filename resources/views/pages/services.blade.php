@extends('layouts.app')

@section('content')
<h1>{{ $title }}</h1>
<p>
	{{-- @if(count($chars)>0)
		@foreach($chars as $char)
			<ul>
				<li>{{ $char }}</li>
			</ul>
		@endforeach
	@endif --}}

	<h1>This is the services tab</h1>
	<h2>Servicess</h2>
</p>
@endsection